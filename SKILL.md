---
name: capcut-marketing-video
description: 用户要求生成视频、编辑视频或由 AI 自动剪辑视频时优先使用。按脚本步骤完成口播、配音、素材、字幕字体、动画、花字、气泡、剪映工程安装及视频验收；也处理文稿和已有草稿导出。
metadata:
  version: "3.1.0"
---

# AI 视频制作与编辑

实际制作视频时先进入本 skill，再选择工具。不以调用 `doctor`、读过本文件或生成 MP4 作为“按 skill 完成”。本技能用 `capcut-cli` 构建/编辑工程、FFmpeg 检查/预处理、剪映原生朗读和导出；原生不可用时在既有授权范围内替代，仍完成约定的工程交付。外部 TTS 仅可用于测试，不得进入营销成片。

## 第一步：分析任务并建立步骤

先从用户原话与当前任务上下文确定：新制作/编辑/导出/仅文稿；交付物；素材及已有草稿；文案、语速和字体要求；已经允许的替代。沿用既有授权，不反复询问可自主决定的选择。缺信息时先做不依赖它的步骤。

第一条业务命令是下列 `start`，保存原始请求和步骤到项目目录。已存在 `task.json` 时读取并继续，不重建、不清掉进度。`<skill>`、`<project>` 等必须替换为真实绝对路径。

```bash
python3 <skill>/scripts/workflow.py start <project> --mode create --delivery draft+native --request '用户本次实际请求及必要的上下文'
```

- `--mode`：`create` / `edit` / `export` / `script`，只走对应步骤，不能为导出重建整个视频。
- `--delivery draft`：明确只要剪映工程；`draft+native`：工程＋视频，包括“草稿视频/先看效果”。近似预览仍用 `draft+native` 保留工程义务，最终另记录输出方式；`video-only`仅用于用户明确仅MP4，不安装但仍从内部工程构建。
- 默认交付**已安装的可编辑剪映工程＋视频**；“剪映草稿/只做草稿”交付已安装工程；“只写口播文稿”止于文稿。只有明确“仅MP4、不要工程”才省略工程安装，并记录原话，不自行从“草稿视频”推断。
- 在 `task.json` 补充已确认的约束和替代授权。向用户用一句话说明本次交付物，继续执行，不把每一步变成确认问题。

## 第二步起：按任务执行

### 新制作：按顺序走完以下步骤

| 步骤 | 优先执行方式 | 此步完成的证据 / 未完成时下一步 |
| --- | --- | --- |
| 1 文稿 | 读 [production.md](references/production.md) 文稿部分，保存原文、断句、停顿、重音 | `storyboard.source_text` 与 `script`；预计时间明确标记，纯文稿可在此交付 |
| 2 素材 | `inspect_inputs.py --media-dir <素材目录>`；候选再加 `--frames-out` | 盘点/抽帧后实际审阅原声、烧录字幕和文意；脚本元数据不能替代判断 |
| 3 配音 | **只在剪映内按最终文案生成**，每句一条原生 `textReading` 音频；禁止外部 TTS、混用旧口播或手动压缩 | `narration.mode` 必填；有口播必须 `aligned`、逐文件实测时长；音频一致性通过后才能 build |
| 4 分镜和设计 | 按实际配音填一份 storyboard，读 [design-recipes.md](references/design-recipes.md) | 镜头、字幕、音频、字体、文意动画、关键词花字、气泡；不要分别维护另一套字幕时轴 |
| 5 准备 | `workflow.py prepare <storyboard.json> --out <project>/builds` | 自动验证并派生 compile.json、voice-script.txt、tts.txt、待处理样式清单；保存返回的 run_dir |
| 6 构建 | `workflow.py build <storyboard.json> --out <project>/builds` | **实际执行 capcut compile**，留下命令、返回结果、refs、草稿及哈希；只有 MP4 不算完成 |
| 7 样式 | `workflow.py finish <run_dir> --resources <native-resources.json>` | 脚本将真实字体、原生进出场、关键词样式及原生圆角气泡写入独立 finished 工程，并读回检查；复杂样式用已核实的CLI扩展 |
| 8 安装 | `workflow.py install <run_dir> --store <实际剪映草稿库>` | 自动复制/收集素材、备份索引、重定位、register计划/执行、回读首页索引和素材；工作目录JSON不等于已安装 |
| 9 验收/导出 | 原生预览、配音和混音试听；按需原生导出，详见 [draft-and-export.md](references/draft-and-export.md) | 检查开头、最长句、CTA与入场/停留/出场；保存证据文件及当前工程哈希；近似输出独立标记 |
| 10 交付检查 | `workflow.py verify <run_dir> --evidence <evidence.json>` | 返回真实检查和 `next_step`；失败就补第一项缺口，不手写 passed 盖过失败 |

运行参数、分镜扩展、证据格式及失败恢复只在 [workflow-p0.md](references/workflow-p0.md) 维护。`start` 是步骤清单；`verify` 根据当前文件重查，不是靠 AI 勾选清单。`finish` 会拒绝缺失或空的动画资源路径，样式门禁会拒绝无主画面、隐藏/出屏字幕；`install` 使用带运行标记的事务副本，注册临时失败可安全重试。

### 编辑已有视频/草稿

只有MP4、没有可编辑工程时走create链建立工程，task.user_request明确保留“只修改哪些内容”的范围；不要伪造一个原生草稿来源。

1. `start --mode edit` 记录指定修改范围。用 `inspect_inputs.py --draft <草稿>` 检查候选，按 [draft-and-export.md](references/draft-and-export.md) 选定最新内容并用 `snapshot_draft.py` 留快照。
2. 优先用已安装的 `capcut-cli` 在独立副本执行具体修改。先读本机命令描述、保存实际执行命令/结果，再读回差异；不写一套 FFmpeg 视频后声称剪映工程已修改。
3. 单纯更换样式复用正确配音；变速重新对齐字幕和画面；保护用户未要求修改的轨道与样式。需要兼容性修复时从快照另建副本，保留失败记录，不伪造一次成功的 compile。
4. 按 [draft-installation.md](references/draft-installation.md) 安装/注册修改副本，运行 `check_installed_draft.py` 和 `capcut lint`，再原生预览/按需导出。当前 `finish/install/verify` 自动链仅支持本次 create 生成的 run_dir；**不得伪造 compile 日志来接入已有工程**。已有工程沿用快照、CLI变更记录、安装检查和视觉/音频证据，逐项报告。

### 仅导出 / 仅文稿

`export`：检查最新候选 → 快照 → 原生导出 → 来源/音轨/完整解码检查；不重新排字或换音。
`script`：保存原文 → 口语化/停顿/重音 → 预计时间 → 文稿交付；不要求素材、配音和工程。

文稿交付偏好：原文信息不得删减，只修正明确错别字、标点和断句；重点在口播行后标注 `（注意点：...）`，该标注不进入配音文本。默认用带唯一 ID 和预计开始时间的脚本格式交付；用户指定语速时按指定倍速重算时间轴（本次偏好为 1.5 倍）。

文稿返回模板：

```text
处理说明：保留原文信息；修正错别字、标点和断句；时间为预计开始时间；“（注意点：……）”不参与口播。
语速：{倍速}；预计总时长：{时长}

S01｜({开始时间})
{口播文本}
（注意点：{重点、语气或画面提示}）

S02｜({开始时间})
{口播文本}
（注意点：{重点、语气或画面提示}）
```

素材分析逻辑：先盘点文件元数据，再筛选清晰、主体完整且可用的素材；对候选视频抽取开头、中段、结尾关键帧并实际审阅；按文稿的钩子、痛点、货源证明、产品优势和 CTA 匹配镜头；用唯一 `script_id` 关联镜头、口播和字幕；最后检查画面与注意点、字幕遮挡和时间轴。素材不足时标记待补，不虚构画面。

## 执行规则

- **现有脚本优先。** 先看本技能脚本及本机 capcut 能力；已覆盖的步骤必须调用，不重新写同功能临时脚本。脚本失败先检查真实错误，修复输入或兼容性；不可绕开校验器直接造另一套工程或MP4。确有未覆盖能力时才写最小项目内脚本，记录缺口、输入/输出和验证，并回到后续检查。
- **脚本未覆盖不等于可省略。** 语义选片、听音和审美由 AI 实际完成，原生朗读/预览/导出由可用应用工具执行；没有能力时标明具体待办，不把字段写入或文件存在称作视觉/听觉通过。
- **分阶段如实交付。** compiled、finished、installed、native_preview、export 是不同状态。最终回答先列已存在的文件和项目名，再列实质未完成项。verify 的失败输出不是审批流程；继续独立工作，已有替代授权不重新问。
- 草稿库写入遵守系统权限；不覆盖同名项目、不恢复用户已删除项目、不批量修改全库、不强制关闭剪映。详见 [draft-installation.md](references/draft-installation.md)。

## 默认设计与交付约束

- 新建9:16竖屏，1080×1920、30fps；源仅720p时可720×1280。已有项目保留画幅。手机观看格式不代表手机版剪映工程兼容。
- **悠然体**：实际文件、内部family及原生字体ID必须对应；文本顶层和富文本style同时设置。字号按字形和最长行校准，不把ASS像素字号等同原生字号；保持手机安全边距，不用大片遮罩。
- 正文入场/出场默认**各0.5秒**；按文意选择原生资源。快速口播先合并意群或补停顿，留稳定阅读，不擅自缩为0.1秒。语速倍数和成片总时长分别记录。
- 默认两者均有：少量**关键词花字与气泡**；统一字体和色板，不逐句堆叠。气泡字号=对应正文−2，底图随文字适配。`finish`提供关键词着色/加粗/描边和原生圆角气泡基础实现；更复杂模板/尾巴等须实际实现后再宣称完成。
- “快速/草稿/你决定”和替代渲染不取消样式质量。只有用户明确的简化要求可填 `style_exceptions`，不得由AI自己写理由跳过要求。
- 原生朗读优先；替换音频完整生成、可解析并覆盖文稿后才替换旧音轨。营销视频只接受剪映按最终文案生成的原生 `textReading` 音频，禁止外部 TTS、混源或手动压缩。新配音重测时长，原声/新口播不冲突，配乐不过响。
- 原生配音按脚本逐句处理：每个 `script_id` 的最终口播文本单独提交给剪映自带 TTS，生成一段独立音频，再用同一 `script_id` 绑定音频、字幕和镜头；不得把多句合并成一段后再切分。
- 用户品牌、数字、优惠、专名及已确认文案优先；不为通过校验删句或擅改。原文与当前口播分开保留。
- 原生导出是最终同款依据。已获授权的近似预览可继续，但不能假称原生效果、不能省略工程安装，不能宣称与原生草稿像素一致；不绕过字体/效果许可。只有明确仅MP4任务例外。

工具边界见 [backends.md](references/backends.md)。技能脚本不提供原生渲染引擎，也不能证明模型未绕过入口；完成结论必须由产物检查与真实审阅证据共同支持。
